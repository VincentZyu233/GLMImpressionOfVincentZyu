# 🔹 行尾统一：仓库内一律 LF，免受 Windows autocrlf 搅扰
* text=auto eol=lf

# 🔹 语言统计：所有 Markdown 强制计入（GitHub Linguist 默认把
#    README 等"文档类"排除在语言统计之外，这里显式拉回来）
*.md linguist-documentation=false
*.md linguist-generated=false
*.md linguist-vendored=false
# prose 类型语言默认不可检测（不进语言条），必须显式打开
*.md linguist-detectable=true

# 🔹 锁文件不参与统计（约 250KB 生成物，会把 JSON 顶成第一大语言，失真）
package-lock.json linguist-generated=true

# 🔹 JSON 是 data 类型语言，默认不可检测，显式打开（tsconfig 等计入）
*.json linguist-detectable=true
*.html linguist-detectable=true
*.js linguist-detectable=true

# 🔹 二进制文件标记
*.png binary
*.jpg binary
*.gif binary
*.ico binary
*.woff2 binary
