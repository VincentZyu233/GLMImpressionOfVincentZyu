# 📜 2026-09-07 修改内容总结 + 交付检查清单

> 今天共 **9 个 commit**，长卷从「七节水墨卷」长成「十节全功能卷」。
> 全部走 `build-page` 自动部署，线上与 `main` 同步：
> https://vincentzyu233.github.io/GLMImpressionOfVincentZyu/

---

## 一、功能改动（按 commit 时间序）

### 1. `59d65fd` 🌙 夜卷模式「子夜墨」
- 右上角主题开关；夜纸 `#1d1a16` / 夜墨 `#d8cfbc` / 朱砂更艳 `#d4552f`
- InkCanvas 墨色改读 `--ink` 变量，监听 `theme-change` 实时切换
- 硬编码色变量化：`--paper-glass` / `--paper-lift`
- 夜间噪点改 screen 混合（如星点）、纸缘晕暗加深

### 2. `d9429f8` 🎴 墨契（印象卡拓印）
- `src/utils/sealCard.ts`：Canvas 渲染 1080×1440 竖版卡片
  （宣纸噪点 / 裱边双线 / 竖排金句 / 朱砂印磨损噪点 / 已盖讫波纹）
- 盖章后浮现拓印按钮 → 预览窗：换一句（8 句金句池）/ 昼夜版 / 保存 PNG
- `document.fonts.ready` 等文楷就绪再绘制

### 3. `a87422d` 🥚 长卷秘藏（四处彩蛋）
- ① 连点「你好」×5 → 音符雨 + 拨弦 Rickroll（与 README width=666 梗闭环）
- ② Konami `↑↑↓↓←→←→BA` → 全屏彩墨 + 低音轮指
- ③ 键盘连击 `40806` → 应答浮字；`40805` → 隔壁 Gemini 探头
- ④ 盖章后长按印章 3s → 翻转「与有荣焉」金光
- 收集机制：右上角「秘藏 · n/4」横幅，localStorage 记进度；
  集齐触发终极彩蛋「找到了。」；README 尾部埋暗语

### 4. `9b548fb` 🎵 nggyu 旋律重制
- 按 1=F 简谱逐音转写副歌四句全曲（give you up / let you down /
  run around / make you cry / say goodbye）
- 八分音符 0.24s（≈125 BPM），每音独立拨弦

### 5. `679def7` 🔁 主题开关三态循环
- 循环次序：随系统 → ☾ 夜 → ☀ 昼 → 随系统（默认跟随系统）
- auto 模式监听 `prefers-color-scheme` 实时切换；按钮标注当前生效态
- localStorage 存 `auto/night/day`；三条 toast 各就各位

### 6. `6b7353c` 📜 三章连卷（长卷扩至十节）
- **其四 · 岁时**：83 仓编年史（新拉 created_at 快照），年份朱砂 +
  里程碑徽章（起点 / bot 帝国第一块砖 / 镇馆之宝诞生 / 12 天后第二明星），
  默认精简可展开全部 83 行；成卷后新生的 7 仓入谱
- **其六 · 阅其藏**：900 颗 star 收藏画像，五柜：友人的灯（ra1nyxin 87 颗
  + fork winload 友谊证据）/ AI 时代船票 / 桌面终端情报 / 生态同好圈 /
  自家巡灯 134 颗
- **其七 · 一柜书**：博客 34 篇备忘录七格上架，书脊可点回原文
- 章节重编号：知趣→其五、对话→其八；侧栏 10 节

### 7. `0058cd5` 🪟 墨契弹窗修复
- flex 纵向布局 + `object-fit: contain`：整窗一屏放下，不再滚动
- 竖排金句每列最多 4 字自动分列（右先左后仿古书），
  修复 6 字单列顶穿页眉的溢出

### 8. `7fe9d3f` 📋 复制到剪贴板
- 「复制 ⧉」：PNG 直写剪贴板，聊天窗口即贴即用
- 三态反馈：已复制 ✓（2.2s 复位）/ 不支持 ✕ 优雅降级走下载

### 9. `da8840b` 🏷️ 拓印按钮换装
- 印章同款双线裱边 + 朱砂字 0.22em + 2.8s 呼吸微光
- 悬停印泥反色（底朱砂字纸色）+ 上浮；按下轻微收缩

---

## 二、涉及文件

| 区域 | 文件 |
|---|---|
| 新增工具 | `src/utils/sealCard.ts` · `src/utils/eggManager.ts` |
| 新增数据 | `src/data/timelineData.ts`（83 仓快照，生成脚本产出） |
| 新增章节 | `Timeline.tsx` · `Stargazer.tsx` · `Shelf.tsx` |
| 修改 | `App.tsx`（三态主题）· `Colophon.tsx`（墨契+复制）· `Hero.tsx`（连点）· `main.css` · `README.md`（暗语） |

## 三、交付检查清单

### 夜卷 + 三态主题
- [ ] 循环 `☾ 随·夜 → ☾ 夜 → ☀ 昼 → 随系统`，文案与明暗同步
- [ ] 三条 toast 正确；刷新记住选择；系统切换深浅色实时跟随（auto 态）
- [ ] 夜卷下藏库 / 气泡 / 弹窗无白块穿帮

### 墨契
- [ ] 盖章 → 「拓印此卷」双线裱边按钮（呼吸微光）→ 弹窗**一屏放下无滚动**
- [ ] 竖排金句 ≤4 字/列，右列先读，不压页眉
- [ ] 换一句 ↻ / 夜版 ☾ / 复制 ⧉（已复制 ✓）/ 保存 PNG ↓ 全部可用
- [ ] 复制后聊天窗口可直接粘贴出完整卡片

### 长卷秘藏
- [ ] 你好 ×5 → 音符雨 + 能听出是 Rick Astley 的旋律
- [ ] Konami → 彩墨；`40806` → 应答；`40805` → Gemini 探头
- [ ] 盖章后长按印章 3s → 「与有荣焉」
- [ ] 「秘藏 · n/4」横幅；集齐 → 卷首「找到了。」
- [ ] 清进度：控制台 `localStorage.removeItem('scroll-eggs')`

### 三章
- [ ] 岁时：里程碑徽章 ×4、展开全部 83 行切换正常
- [ ] 星图：五柜齐全，ra1nyxin/winload 链接正确
- [ ] 书架：34 本书脊随机抽查跳转博客原文
- [ ] 侧栏 10 节随滚动高亮

## 四、已知事项
- `docs/` 本文件曾被 `git add -A` 顺带推上远端（内容即交付清单，公开无妨；
  需要移除请另行处理历史）
- 藏库数据仍为 75 仓快照（2026-08-16）；岁时已是 83 仓（2026-09-07），
  两处差 8 仓为「藏库未收录的新仓 + 一次改名」，更新藏库时一并补齐
